const Team = require("./Team2");

// Home and Away both Team object
function Game2 (home, away) {

  this.home = home;
  this.away = away;

  // [int, int, ...]
  this.getInputs = function () {};

  // [p1, p2]
  this.getProbs = function () {};

}
